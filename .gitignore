# Исключаем переменные окружения (секреты)
.env
.env.local

# Исключаем установленные модули
node_modules/

# Локальное хранилище сервисов (если не используется MongoDB)
data/

# Логи и временные файлы
npm-debug.log*
yarn-debug.log*
yarn-error.log*
